# Development image: Vite dev server with HMR.
FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173
# --host is also set in vite.config.js; passed here for clarity.
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
